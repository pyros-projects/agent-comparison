[project]
name = "orpheus"
version = "0.1.0"
description = "Event-driven job orchestrator with API and worker"
readme = "README.md"
requires-python = ">=3.11"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
